#include<iostream>
#include<vector>
using namespace std;

int main() {
	// int arr[100];
	// vector<datatype> nameofvector;
	vector<int> v;
	// vectorname.push_back(ele)
	v.push_back(4);
	v.push_back(3);
	v.push_back(8);
	// vectorname.size();//no of ele present
	// cout<<v.size()<<endl;//3
	// // vectorname.capacity();//

	// cout<<v.capacity()<<endl;//4

	v.push_back(9);

	v.push_back(1);
	// cout<<v.size()<<endl;//5
	// // vectorname.capacity();//

	// cout<<v.capacity()<<endl;//8

	// for(int i=0;i<v.size();i++){
	// 	cout<<v[i]<<" ";
	// }
	// cout<<endl;

	// vectorname.pop_back()
	// v.pop_back();

	// cout<<v.size()<<endl;//5
	// // vectorname.capacity();//

	// cout<<v.capacity()<<endl;//8

	// for(int i=0;i<v.size();i++){
	// 	cout<<v[i]<<" ";
	// }
	// cout<<endl;


// cout<<v.front()<<endl;//
// cout<<v.back()<<endl;//

// v.insert(v.begin(),5);
// 	v.insert(v.begin()+3,5);

// for(int i=0;i<v.size();i++){
// 		cout<<v[i]<<" ";
// 	}
// 	cout<<endl;


	// v.erase(v.begin()+2);
	// v.clear();

	// cout<<v.capacity()<<endl;//8













	return 0;
}